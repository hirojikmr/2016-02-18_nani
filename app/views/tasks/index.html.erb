<p id="notice"><%= notice %></p>

<h1>Listing Tasks</h1>

<table class="data">
  <thead>
    <tr>
      <th>Start</th>
      <th>End</th>
      <th>Dur</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.start.strftime "%m/%m %H:%M:%S" %></td>
        <td><%= task.end.strftime " %H:%M:%S"  %></td>
        <td><%= task.dur.utc.strftime " %H:%M:%S" unless task.dur.nil? %></td>
        <td><%= task.body %></td>
        <td><%= link_to 'Show', task %></td>
        <td><%= link_to 'Edit', edit_task_path(task) %></td>
        <td><%= link_to 'Destroy', task, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Task', new_task_path %>
<%= link_to 'START', :action=>"start"%>
