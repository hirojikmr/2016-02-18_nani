<%= form_for(@task_color) do |f| %>
  <% if @task_color.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@task_color.errors.count, "error") %> prohibited this task_color from being saved:</h2>

      <ul>
      <% @task_color.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_field :text %>
  </div>
  <div class="field">
    <%= f.label :color %><br>
    <%= f.color_field :color %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
